const numbers = [1, 2, 3, 4, 5];
console.log(numbers);

for(const num of  numbers) {
    console.log(num);
};

for (let i = 1; i < 1111; i++) {
    console.log(" I love you " + i); // Output: 0, 1, 2, 3, 4
}
